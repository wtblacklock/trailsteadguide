# Style rules

- Never use em dashes (—) or en dashes (–) anywhere in site content (guide/compare/plan copy, JSX text, metadata, JSON-LD strings, etc). Use a plain hyphen (`-`) instead, or restructure with a comma, colon, or period. This applies to new content and edits to existing content alike; do not reintroduce them.
