# Routine health check - 2026-09-21

Weekly review of the other scheduled Routines on this account: did they actually produce
signal, not just a SUCCEEDED status.

## Summary

Every Routine fired on schedule (or is not yet due) and produced real output - none of this
week's runs look like the silent no-op that motivated this check. But two things need a human:

1. **A pull-request merge backlog has built up behind every Routine.** 16 PRs are open, the
   oldest from 2026-09-03 (18 days). Two are weekly site-health audits (#93, #100) and two are
   dependency upkeep (#90, #98). Merge cadence dropped from near-daily through 2026-09-07 to
   roughly two merges in the last two weeks. Verified defects from PR #93 (three factual errors,
   17 over-length SEO titles) are still live on production because that PR hasn't merged.
2. **The Dependency & Security Upkeep session is stuck blocked**, needing human approval for an
   automated check-in on PR #98 that it can't send.

Neither is a Routine failing to run its checks. Both are downstream of the same cause: nothing is
merging PRs right now.

Egress to third-party hosts (amazon.com, recreation.gov, etc.) is still blocked by the session
network policy, same as the 2026-09-04 incident that prompted this health check. That is not new,
but it is worth restating because it degrades several Routines' checks every week until it's
either lifted or the Routines are made to positively report the block (see recommendation 3).

## Anomalies found

### 1. PR merge backlog (affects: weekly gap + site health audit, Dependency & Security Upkeep, Content Growth)

- 16 open PRs as of this check, oldest `#74` (fix/gear-shelf-inline-mismatch) opened
  2026-09-03T16:07:53Z - 18 days old.
- Merge history: near-daily merges from 2026-08-31 through `#85` on 2026-09-07T17:52:49Z. Then a
  gap until `#79`/`#94` merged together on 2026-09-15T04:0x, then another gap until `#99` merged
  2026-09-21T12:31:17Z. Two merges in the last two weeks versus roughly one per day before that.
- Weekly site-audit PRs `#93` (2026-09-14) and `#100` (2026-09-21) are both still open, so
  `docs/site-audit/2026-09-14.md` and `docs/site-audit/2026-09-21.md` exist only on their branches,
  not on `main` (main has only 2026-09-03.md and 2026-09-07.md). This is why a check of `main`
  alone would have wrongly looked like two missing weekly audits - both were confirmed present and
  substantive (391 and 351 lines) on their PR branches.
- Weekly dependency-audit PRs `#90` (2026-09-10) and `#98` (2026-09-17) are both still open for the
  same reason; `docs/dependency-audit/` does not exist on `main` at all yet, despite two real audit
  files (162 and 111 lines) sitting on those branches.
- `#100`'s own audit report (written by the weekly gap-audit Routine this run) independently
  reached the same conclusion and re-verified that three factual errors and 17 SEO-title
  violations fixed in `#93` are still live on production a week later. It is the strongest
  evidence here and is worth reading directly on the `audit/2026-09-21` branch.
- Evidence this is a review bottleneck, not a Routine problem: every PR checked (`#98`, and per
  the `#100` report, `#93`) has `mergeable_state: clean` and green CI (lint, tests, Vercel preview).
  Nothing is blocking the merge except a human clicking merge.

### 2. Dependency & Security Upkeep session blocked on its own follow-up (session_0188GTVRMjG3drDQRB4jhUqd)

- The 2026-09-17 run itself succeeded (fired 13:10:31Z, finished 15:04:26Z, opened PR #98, CI
  green). That part is fine.
- The persistent session is currently `status_bucket: BLOCKED` / `session_status:
  SESSION_STATUS_REQUIRES_ACTION`, unread, with a pending `send_later` tool call awaiting
  permission approval. The call is a scheduled check-in on PR #98's merge status; per its own
  queued message, one reminder notification has already gone out (~2026-09-21) and the PR has been
  quiet since 2026-09-17T15:01:54Z.
- Net effect: the Routine's own escalation path for "PR sitting unreviewed" is itself sitting
  unapproved. A human needs to either approve that pending action or merge #98 directly (it is
  already mergeable and green).

### 3. Trailstead Gear Catalog Health (2026-09-18, session_01WcUTGoFGTpXsVRhPcfoLk3) - unconfirmed signal

- Fired 2026-09-18T12:45:57Z, finished 12:49:15Z - **198 seconds**, just over this check's ~3-minute
  plausibility floor and well short of the 10-17 minute range every other weekly Routine took this
  week.
- Produced 17,046 output tokens and cost $1.19 - more work than the 2026-09-04 incident (~5,300
  tokens, 109s), but still left **no branch, no PR, and no report file** behind, so there is
  nothing in the repo to check its findings against.
- Egress to amazon.com is confirmed still blocked this week (see Egress posture below) - the same
  condition that caused the 2026-09-04 run to silently report healthy while unable to reach the
  one host it exists to check.
- Unlike the weekly gap-audit Routine (which now explicitly documents the egress block and what it
  couldn't verify, in its own report file), Gear Catalog Health leaves no artifact, so there is no
  way from outside the session to confirm whether this run detected the same block and said so, or
  repeated the 2026-09-04 silent pass. Recommend giving this Routine the same kind of durable
  report file the audit and dependency Routines have, specifically so this question is answerable
  without relying on session internals.

## Not flagged as anomalies (checked, found fine)

- **Trailstead Annual Seasonal Accuracy Sweep**: never fired, `last_run` is empty. Expected - its
  cron (`0 14 20 2 *`) isn't due until 2027-02-20.
- **Trailstead SEO & Technical Maintenance** (2026-09-21, session_019WTe468zceoLRY3FYFg4rz):
  fired/finished today, ~13m24s, PR #99 merged same day. Healthy.
- **Trailstead - weekly gap + site health audit** (2026-09-21, session_01TzE2y2XJTP4Kq4mSKFsMcN):
  ~13m16s, produced a substantive, self-aware 351-line report on PR #100 (see anomaly 1). Healthy
  Routine behavior; the backlog it's caught in is the anomaly, not the Routine itself.
- **Trailstead Content Growth** (2026-09-16, session_01Y9w93o2fTz8id7JdF9ERuS): ~10m23s, opened PR
  #95 (Junior Ranger printable). Healthy, though also stuck in the same PR backlog.
- **SEO Maintenance vs. weekly gap audit same-day scheduling**: both fire Monday (12:15 and 14:00
  UTC, ~1h45m apart). Checked for collision risk: both work through PR branches rather than pushing
  straight to `main`, and this week's runs (`#99`, `#100`) did not conflict. Not currently a risk,
  worth re-checking if either Routine is ever changed to push directly to `main`.

## Egress posture

- Agent proxy is enabled and reachable (`$HTTPS_PROXY/__agentproxy/status` responds normally,
  `bundleCoversEveryHost: true`, no recent relay failures logged).
- Direct test of two representative external hosts both failed with `connect_rejected` (organization
  policy) / HTTP `000`:
  - `https://www.amazon.com/` - blocked
  - `https://www.recreation.gov/` - blocked
- This matches what `#100`'s own audit reports: external checks (dead links, dead images,
  affiliate-link health - 414 external URLs / 205 image URLs) have now been unverifiable for four
  consecutive weekly audits. The policy has not been widened since the 2026-09-04 incident.

## Recommended action for a human

1. **Clear the PR backlog**, starting with `#93` and `#98` - both are clean/green and have been
   waiting the longest, and `#93` is confirmed to be holding back three live factual errors on
   production. Then work through the rest (`#74` is the oldest at 18 days).
2. **Approve or resolve the pending action on session_0188GTVRMjG3drDQRB4jhUqd** (Dependency &
   Security Upkeep) - either let its queued PR-#98 check-in send, or just merge #98 and let the
   session close out on its own.
3. **Decide on the egress policy**: either widen it to let these Routines reach the external hosts
   they exist to check, or explicitly accept the block and require every Routine that depends on
   external network access (Gear Catalog Health included) to write a durable report file the way
   the site-audit and dependency-audit Routines already do, so a blocked run is provably
   distinguishable from a healthy one without needing session internals.
